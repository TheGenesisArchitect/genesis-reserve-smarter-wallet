 
 GENESIS RESERVE `n OFFICIAL TECHNICAL SPECIFICATIONS `n ───────────────────────────────── `n Enterprise Dashboard · All-In-One Digital Wallet · Yield Engine `n MVP Launch v2.0 · Q2 2026 `n SECTION I Engineers SECTION II Business Partners SECTION III Investors `n Confidential · March 26, 2026 · anthony@genesisreserve.io `n `n ARCHITECTURE SIGN-OFF REVIEW `n The v2 architecture diagram has been validated against the live codebase (12,517 lines across 4 layers). This section records the findings that must be resolved before the diagram can be stamped APPROVED FOR IMPLEMENTATION . `n `n Smart Contracts `n LIVE — Arbitrum One `n Backend API `n COMPLETE — 19 routes `n Frontend Layer `n COMPLETE — all hooks/UI `n BFF Aggregate Layer `n NOT BUILT — Week 1 `n Partner Admin Console `n NOT BUILT — Week 2 `n Feature Flags + Notifications `n NOT BUILT — Week 1 `n `n Sign-Off Verdict: CONDITIONAL APPROVAL `n DECISION: The architecture diagram accurately represents the TARGET state of the system. All built components are correctly depicted and validated against live contract addresses. The 6 missing components (BFF layer, feature flags, notifications, account ownership guard, partner admin console, PgBouncer ) are correctly shown as required but not yet built. The diagram is approved as the implementation contract with the condition that all 6 missing components are completed before launch-day sign-off. `n `n Architecture Accuracy by Layer `n LAYER `n DIAGRAM CLAIM `n ACTUAL STATUS `n SIGN-OFF `n GenesisVault.sol / StrategyRouter.sol `n Live Arbitrum One `n Confirmed — 0xe164... / 0xD7ff... `n `n 4 Protocol Adapters `n Aave /Balancer/T-Bills/Euler `n Deployed + registered. Euler EVault needs on-chain verification. `n `n ComplianceRegistry.sol `n On-chain KYC state `n Confirmed — 0x6D58... live on Arbitrum One `n `n Backend API (Express) `n 19 routes across 5 routers `n Confirmed — treasury/remittance/ledger/compliance/webhooks `n `n Compliance Services `n 4-gate KYC/AML/OFAC/ TravelRule `n Confirmed — compliance.service .ts , Onfido / Chainalysis webhooks `n `n Harvest Cron `n 5-min epoch trigger `n Confirmed — PagerDuty/Slack/Datadog wired `n `n Reconciliation Service `n Hourly + stuck reservation cleanup `n Confirmed — $0.01/$1.00/$10.00 alert thresholds `n `n Yield Engine Frontend `n 4 hooks + WebSocket + dashboard `n Confirmed — useYieldEngine + YieldEngineDashboard.tsx `n `n BFF / api /gr/* routes `n Aggregate endpoints, not passthrough `n NOT BUILT — Week 1 sprint target `n `n Feature Flag Middleware `n partner_flags Redis key, 5-min TTL `n NOT BUILT — partner_feature_flags table needed `n `n Account Ownership Guard `n account_id + privy_did match `n NOT BUILT — auth middleware needed `n `n Notification Service `n EventBus subscriber → notifications table `n NOT BUILT — table + subscriber needed `n `n Partner Admin Console `n /admin/* route, 7 pages `n NOT BUILT — Week 2 sprint target `n `n PgBouncer `n pool_mode =transaction, max 500 `n NOT IN docker-compose — must add before load test `n `n MoneyGram / CCTP `n Live payout integration `n STUB — must resolve before real user send flow `n `n `n `n `n `n SECTION I — ENGINEERS `n Full-stack technical specification · Smart contracts · Backend services · Frontend architecture · Data models · API contracts `n `n 1.1 SYSTEM OVERVIEW `n Genesis Reserve is a sovereign treasury infrastructure platform built on Arbitrum One. At its core is a live ERC-4626 vault ( GenesisVault.sol ) that deploys USDC across 4 DeFi protocol adapters, harvesting yield every 15 minutes. A full-stack application layer (Next.js BFF → Express API → PostgreSQL + Redis) exposes this infrastructure to business partners and end users through a compliant, auditable interface. `n `n DIMENSION `n SPECIFICATION `n Codebase Size `n 12,517 total lines — 2,741 Solidity / 4,103 TypeScript backend / 4,190 TypeScript frontend / 1,483 tests `n Blockchain `n Arbitrum One ( chainId 42161) — EVM-compatible L2, ~250ms block time, ~$0.001/ tx gas `n Target Scale `n 10 partners / 10,000 users / 100k+ monthly transactions / $500M AUM `n Availability SLA `n 99.5% uptime — P50 <200ms, P95 <500ms, P99 <1,000ms `n Security Model `n ERC-4337 smart accounts ( ZeroDev Kernel v3. 1) — users never sign raw transactions `n Compliance `n 4-gate KYC/AML/OFAC/ TravelRule — Onfido + Chainalysis — TRISA protocol for >$3k `n Yield Engine `n 15-min harvest epochs — 5 data sources — WebSocket real-time — priority APY waterfall `n Database `n PostgreSQL 15 + Redis 7 + Kafka ( KRaft ) — 15 tables — hash-chained ledger — ISO 20022 export `n `n 1.2 SYSTEM ARCHITECTURE — LAYER DIAGRAM `n `n CLIENT `n End Users 10k concurrent `n Privy embedded wallet `n ↓ `n Partner Admins 10 partners `n Per-partner console `n ↓ `n Genesis Ops `n Reconciliation + yield `n ↓ `n Monitoring Datadog + PagerDuty `n 4 error metric streams `n `n FRONTEND `n Dashboard + Yield Engine `n WalletBalance + YieldEngDash `n ↓ `n Deposit + Send Flows `n Gasless ERC-4337 `n ↓ `n History + Ledger `n CSV export `n ↓ `n Partner Admin /admin/* ⚠ `n Week 2 build `n `n `n BFF `n ⚠ WEEK 1 BUILD TARGET — Not yet implemented `n Auth Middleware Privy JWT→claims `n ↓ `n Feature Flags Redis 5m TTL `n ↓ `n Rate Limit 100/min/partner `n ↓ `n Aggregate 7 BFF endpoints `n ↓ `n Account Ownership Guard `n `n `n API `n treasury.service + compliance.service `n reserve/finalize/activate `n ↓ `n remittance.service MoneyGram stub⚠ `n quote/order/status `n ↓ `n ledger.service hash-chained `n ISO 20022 + export `n ↓ `n yield.service + harvest.cron `n 15-min epochs `n ↓ `n reconciliation +webhooks `n hourly invariant `n `n DATA `n PostgreSQL 15 15 tables + 5 pending `n PgBouncer ⚠ required `n ↓ `n Redis 7 6 cache keys `n TTL 10s–5m `n ↓ `n Kafka KRaft 20 event types `n in-process fallback `n ↓ `n Arbitrum One 5 live contracts `n ERC-4626 + adapters `n `n DIAGRAM KEY: ⚠ = Missing component (Week 1 sprint target) | Green = Live on Arbitrum mainnet | Amber = Planned/stubbed | All flow is left-to-right, top-to-bottom. BFF calls API services in parallel via Promise.all ( ) — not sequential. `n `n 1.3 SMART CONTRACT LAYER `n Live Contracts — Arbitrum One ( chainId 42161) `n CONTRACT `n ADDRESS `n LINES `n ROLE `n `n `n 487 `n ERC-4626 yield vault — deposit/withdraw/reserve/finalize `n `n `n 336 `n Capital allocation across adapters — 40 % max per protocol `n `n `n 380 `n On-chain KYC state — activated by Onfido webhook on pass `n `n `n 334 `n INSTANT band — Aave V3 USDC supply — 40% max alloc `n `n `n 514* `n HOURS band — Balancer V3 USDC-USDT pool — 30% max alloc `n `n `n 372 `n HOURS band — Euler Finance v2 EVault — 10% max alloc `n `n * BalancerV3Adapter line count includes MorphoAndBalancerAdapters.sol . ⚠ Euler EVault address requires on-chain verification: cast call 0x797D... " asset( )" -- rpc-url $ALCHEMY_ARB_MAINNET must return USDC address. `n `n Yield Engine — On-Chain Mechanics `n EPOCH PHASE `n TRIGGER `n ACTION `n EMITS `n 1. Running (0–14:59) `n Block time elapses `n Yield accrues passively inside protocols `n — `n 2. Harvest eligible (15:00+) `n block.timestamp ≥ epochStart + 900 `n harvest( ) callable (HARVESTER_ROLE only) `n — `n 3. Harvest execution `n API cron → Operator wallet `n Router calls harvest( ) on each adapter → yield → share price `n YieldHarvested ( epoch, yieldUsdc , apyBps , ts ) `n 4. Rebalance (conditional) `n Allocation drift > 5% from targets `n Capital shifted between adapters `n Rebalanced( epoch, from, to, amount) `n 5. Next epoch `n After harvest completes `n epochNumber ++, epochStartTime = block.timestamp `n — `n `n 1.4 BACKEND SERVICE LAYER `n SERVICE `n FILE `n LINES `n CRITICAL FUNCTIONS `n Treasury `n treasury.service .ts `n 645 `n activateAccount / reserveFunds / finalizeOrder / getBalance / getYieldReport `n Ledger `n ledger.service .ts `n 499 `n postEntry / getEntries / reconcile / exportISO20022 — hash-chained `n Compliance `n compliance.service .ts `n 375 `n screenDeposit / screenTransfer / 4-gate: KYC/OFAC/AML/ TravelRule `n Remittance `n remittance.service .ts `n 261 `n getFXQuote / executePayout (STUB) / checkOrderStatus — 4 corridors `n Yield Engine `n yield.service .ts `n 377 `n getYieldSnapshot / triggerHarvestCheck / computeRollingApy `n API Gateway `n index.ts `n 498 `n 19 routes — rate limit / idempotency / RFC 7807 errors `n Harvest Cron `n harvest.cron .ts `n 274 `n 5-min schedule / PagerDuty on 3x failure / Datadog metrics `n Reconciliation `n reconciliation.service .ts `n 342 `n Hourly invariant check / stuck reservation cleanup / suspension gate `n Onfido Webhook `n onfido.webhook .ts `n 250 `n HMAC-SHA256 verify / DB update / on-chain activation via Operator wallet `n Chainalysis Webhook `n chainalysis.webhook .ts `n 257 `n HMAC-SHA256 verify / SUSPEND on HIGH/CRITICAL alert / audit log `n `n API Routes — Complete Manifest `n METHOD `n ROUTE `n AUTH `n FUNCTION `n GET `n / health `n none `n Liveness probe `n GET `n / ready `n none `n Readiness probe (DB + chain) `n POST `n /v1/treasury/accounts `n api -key + idempotency `n Activate PTA after KYC `n GET `n /v1/treasury/accounts /: accountId `n api -key `n Get account details `n GET `n /v1/treasury/balance /: accountId `n api -key `n Real-time balance (available/reserved/invested) `n POST `n /v1/treasury/reserve `n api -key + idempotency `n Lock funds for outgoing payment `n POST `n /v1/treasury/finalize `n api -key + idempotency `n Settle reservation after payout `n POST `n /v1/treasury/mode `n api -key `n Set TreasuryMode (0=Flexible/1=Income/2=Growth) `n GET `n /v1/treasury/risk `n api -key `n Composite risk report (0–100 score) `n GET `n /v1/treasury/yield /: accountId `n api -key `n Yield history for account `n POST `n /v1/remittance/quote `n api -key + idempotency `n 60-second locked FX quote `n POST `n /v1/remittance/order `n api -key + idempotency `n Execute from locked quote `n GET `n /v1/remittance/order /: orderId `n api -key `n Order status + tracking `n GET `n /v1/ledger/entries /: accountId `n api -key `n Paginated ledger entries `n GET `n /v1/ledger/balance /: accountId `n api -key `n Derived balance from ledger `n GET `n /v1/ledger/export /: accountId `n api -key `n ISO 20022 camt.053 export `n POST `n /v1/ledger/reconcile `n api -key `n On-demand reconciliation `n GET `n /v1/compliance/status /: walletAddress `n api -key `n Compliance record for wallet `n POST `n /v1/compliance/screen `n api -key + idempotency `n On-demand AML/sanctions screen `n POST `n /webhooks/ onfido `n HMAC-SHA256 `n KYC completion → on-chain activation `n POST `n /webhooks/ chainalysis `n HMAC-SHA256 `n AML push alert → account suspension `n `n 1.5 DATABASE SCHEMA `n Existing Tables ( 15) — Migration 001 `n TABLE `n KEY COLUMNS `n PURPOSE `n users `n user_id UUID, email_hash TEXT, status, risk_tier `n Identity + PII-tokenized user records `n identity_cases `n identity_id , user_id , kyc_level , sanction_status , kyc_expiry `n KYC verification state per user `n compliance_screenings `n screening_id , user_id , type, result, screened_at `n Audit trail for every OFAC/AML/velocity check `n treasury_accounts `n account_id ( pta -xxx), owner_id , wallet_address , mode, kyc_level `n Programmable Treasury Accounts (PTAs) `n account_balance_snapshots `n account_id , available, reserved, invested, timestamp `n Point-in-time balance snapshots `n ledger_entries `n entry_id , type, debit_account , credit_account , amount, prev_hash , hash `n Hash-chained double-entry accounting `n remittance_quotes `n quote_id , corridor, send_amount , receive_amount , fx_rate , expires_at `n FX quotes with 60-second lock `n remittance_orders `n order_id , quote_id , account_id , status, provider_ref `n Order lifecycle tracking `n fund_reservations `n reservation_id , account_id , amount, status, expiry_at `n Capital locks for pending payouts `n travel_rule_records `n order_id , sender_address , recipient_address , amount, status `n FinCEN TRISA compliance >$3k `n strategy_positions `n account_id , adapter_address , deployed_usdc , last_harvest `n Per-adapter capital tracking `n yield_accruals `n epoch_number , yield_usdc , apy_bps , total_aum , tx_hash , harvested_at `n Harvest history per epoch `n partners `n partner_id UUID, name, api_key_hash , status, rate_card_id `n Business partner registry `n partner_revenue `n partner_id , period, tx_volume , fee_revenue , yield_share `n Partner revenue tracking `n audit_log `n action, actor, details JSONB, created_at `n Immutable operations log `n `n Required Additions ( 5) — Migration 002 `n TABLE `n KEY COLUMNS `n PURPOSE `n SPRINT `n partner_feature_flags `n partner_id UUID, feature TEXT, enabled BOOL, config JSONB `n Per-partner feature enable/disable `n Week 1 `n notifications `n notification_id UUID, user_id UUID, type TEXT, payload JSONB, read BOOL `n In-app yield + order alerts `n Week 1 `n api_idempotency_keys `n key TEXT PK, partner_id UUID, response JSONB, expires_at TIMESTAMPTZ `n DB-backed idempotency (cross-restart) `n Week 1 `n partner_rate_cards `n partner_id UUID, corridor TEXT, tx_fee_bps INT, fx_spread_bps INT `n Per-partner FX pricing overrides `n Week 3 `n admin_sessions `n session_id UUID, partner_id UUID, ip TEXT, expires_at TIMESTAMPTZ `n Partner admin console auth `n Week 2 `n `n 1.6 FRONTEND ARCHITECTURE `n Hook Hierarchy — Yield Engine `n HOOK `n DEPENDS ON `n READS FROM `n REFRESH `n `n wagmi useReadContracts `n StrategyRouter — 3 reads batched `n 60s poll `n `n viem watchContractEvent `n Alchemy WebSocket → YieldHarvested `n Real-time push `n `n wagmi usePublicClient `n GenesisVault.previewRedeem (1e6) `n 30s + 1s interpolation `n `n All 3 above + useGenesisVault `n Orchestrated — APY priority waterfall `n Derived `n `n APY Data Priority Waterfall `n PRIORITY `n SOURCE `n CONFIDENCE `n BADGE `n 1 — Highest `n `n Real-time realised APY from chain `n `n 2 `n `n Aggregated from StrategyRouter state `n `n 3 `n `n Derived from previewRedeem ( ) change `n `n 4 — Fallback `n `n Only shown while initial data loads (<2s) `n `n `n 1.7 BFF AGGREGATE ENDPOINTS — TO BUILD WEEK 1 `n ENDPOINT `n PARALLEL BACKEND CALLS `n CACHE TTL `n RESPONSE FIELDS `n GET / api /gr/dashboard `n balance + yield/snapshot + compliance/status + epoch + ledger/ entries( 5) `n 30s `n balance, yieldSnapshot , complianceStatus , epochState , recentTx [ 5] `n GET / api /gr/yield `n yield/snapshot + yield/allocations + yield/epoch + harvest/ history( 10) `n 60s `n blendedApy , allocations[ ], epochState , harvestHistory [ 10] `n GET / api /gr/history `n ledger/ entries( cursor, pageSize , filters) `n 10s `n entries[ ], total, nextCursor `n POST / api /gr/send `n quote → compliance/screen → treasury/reserve (sequential) `n no cache `n quoteId , reservationId , status, etaSeconds , complianceResult `n POST / api /gr/deposit `n compliance/screen → on-chain deposit via smart account `n no cache `n txHash , shares, newBalance , confirmed `n GET / api /gr/notifications `n notifications table query `n 10s `n unread: N, items[]: { type, message, createdAt , read} `n GET / api /gr/accounts `n treasury_accounts WHERE owner_id = user_id `n 30s `n accounts[]: { accountId , mode, balance, label} `n `n 1.8 INFRASTRUCTURE & PERFORMANCE `n Redis Cache Strategy `n KEY `n TTL `n INVALIDATION `n LATENCY TARGET `n `n 30s `n On deposit/withdrawal/settlement `n <20ms (cache hit) `n `n 60s `n On YieldHarvested WebSocket event `n <20ms (cache hit) `n `n 5 min `n On Onfido / Chainalysis webhook `n <20ms (cache hit) `n `n 5 min `n On admin flag toggle `n <20ms (cache hit) `n `n 30s `n Time-based expiry only `n <20ms (cache hit) `n `n 10s `n On order status change `n <20ms (cache hit) `n `n Performance Targets & Infrastructure Requirements `n REQUIREMENT `n TARGET `n IMPLEMENTATION `n P50 API latency `n <200ms `n BFF aggregate (parallel calls) + Redis cache on reads `n P95 API latency `n <500ms `n PgBouncer connection pooling ( max_client_ conn =500) required `n P99 API latency `n <1,000ms `n 3 critical PostgreSQL indexes on high-frequency queries `n Concurrent users `n 1,000 (load test) → 10,000 (production) `n PgBouncer + Redis + horizontal API scaling `n Harvest latency `n <5 min from epoch end `n harvest.cron .ts polling every 5 min — PagerDuty on 3x failure `n DB connection pool `n 20 current → 500 with PgBouncer `n Add to docker-compose before Week 3 load test `n WebSocket connections `n Up to 10,000 simultaneous `n Alchemy WebSocket endpoint — Yield Engine real-time feed `n Chain confirmation SLA `n 1 block (~250ms on Arbitrum ) `n useWaitForTransactionReceipt ( confirmations: 1) `n `n 1.9 SECURITY MODEL `n LAYER `n MECHANISM `n ENFORCEMENT POINT `n Smart Account Auth `n ZeroDev Kernel v3.1 ERC-4337 — users sign UserOperations , never raw txs `n Privy embedded wallet + Paymaster `n KYC Gate `n ComplianceRegistry.sol on-chain — activateAccount ( ) only callable by Operator after Onfido pass `n onfido.webhook .ts → chain write `n AML Screening `n 4-gate: KYC tier / OFAC ( Chainalysis ) / velocity (24h) / Travel Rule (>$3k TRISA) `n compliance.service .ts screenTransfer ( ) `n API Authentication `n x- api -key for partners (hashed in DB) + JWT for users (Privy-issued) `n authenticateApiKey middleware `n Idempotency `n DB-backed api_idempotency_keys table — replay-safe across restarts `n requireIdempotencyKey middleware `n Account Ownership `n privy_did + account_id ownership check on every treasury operation `n Account ownership guard (Week 1 build) `n Reconciliation `n Hourly on-chain vs ledger delta check — suspend new ops if >$10 discrepancy `n reconciliation.service .ts `n Feature Flags `n Per-partner feature gates — resolved in BFF session init `n Feature flag middleware (Week 1 build) `n Emergency Pause `n GUARDIAN_ROLE on each adapter — single tx emergency exit to safe address `n emergencyExit ( ) on all IStrategy adapters `n Audit Trail `n Immutable audit_log table — every sensitive operation logged with actor + details `n API middleware + service layer `n `n `n `n `n SECTION II — BUSINESS PARTNERS `n What Genesis Reserve does · How your users experience it · What you control · SLAs and support `n `n 2.1 WHAT GENESIS RESERVE DELIVERS `n Genesis Reserve is the "AWS for stablecoins" — an institutional-grade treasury and payments infrastructure that your business deploys in front of your users. Your users get a digital wallet that earns yield on idle balances and sends money globally. You get a revenue share on every transaction and on the yield generated by your users' deposits. `n `n Your Users Get `n `n Yield on idle USDC — 5.32% blended APY, credited every 15 minutes `n Gasless transactions — no MetaMask popups, one tap to deposit `n Global remittance — US→PH, US→MX, US→IN from a single app `n Real-time balance — per-second interpolation between harvests `n Compliance built in — KYC, AML, sanctions, travel rule automatic `n Full transaction history — ISO 20022 export, on-chain audit trail `n `n You Get (as Partner) `n `n Revenue share — 1.0%/yr of your users' deployed AUM `n TX fee share — tiered 20–40% of 0.42% per transaction `n FX spread share — on each cross-border send `n Admin console — manage users, set KYC tiers, view volume `n Feature control — enable/disable corridors, yield, send per user `n API access — programmatic integration post-MVP `n `n 2.2 REVENUE MODEL — PARTNER ECONOMICS `n REVENUE STREAM `n RATE `n GENESIS SHARE `n PARTNER SHARE `n EXAMPLE `n Yield spread on AUM `n 1.5%/yr on deployed capital `n 0.5%/yr `n 1.0%/yr `n `n Transaction fee `n 0.42% per transfer `n Split by tier `n L1: 0% / L2: 20% / L3: 30% / L4: 40% `n `n FX spread `n 0.25% per cross-border send `n 100% Genesis `n 0% `n `n Card program (post-MVP) `n Interchange + Stripe fees `n TBD `n TBD `n `n `n 2.3 PARTNER ADMIN CONSOLE (WEEK 2 DELIVERY) `n CONSOLE PAGE `n WHAT YOU CAN SEE `n WHAT YOU CAN DO `n Overview `n Transaction volume, fee revenue, yield generated, error rate — current + 30-day trend `n Export CSV, set date range `n Users `n All users under your partner ID — KYC tier, status, last active, lifetime volume `n Invite by email, set KYC tier, suspend/reactivate `n Yield Report `n Yield earned by your users (and your 1.0% share) per day/week/month `n Export yield report `n Feature Flags `n 10 toggles: YIELD_ENGINE, SEND_FLOW, CSV_EXPORT, corridors (PH/MX/IN/NG) `n Enable/disable per feature — effective immediately `n Settings `n API key (last 4 visible), corridor list, rate card view `n Rotate API key, view rate tiers `n Support Queue `n Active compliance holds, failed orders requiring action `n Escalate holds, trigger refunds `n `n 2.4 CORRIDORS, LIMITS & COMPLIANCE `n CORRIDOR `n STATUS `n PAYOUT METHODS `n FX RATE `n SLA `n US → Philippines (PH) `n `n Bank transfer · Cash pickup `n 55.26 PHP/USD (mid-market - 0.25%) `n ~1 hour via MoneyGram `n US → Mexico (MX) `n `n Bank transfer · Mobile wallet · Cash `n 17.12 MXN/USD `n ~2 hours `n US → India (IN) `n `n Bank transfer `n 83.15 INR/USD (CCTP route) `n ~1 hour `n GB → Nigeria (NG) `n `n Bank transfer `n ~1,580 NGN/USD (volatile) `n 24 hours (pending license) `n `n KYC TIER `n DAILY LIMIT `n SEND LIMIT `n REQUIREMENTS `n UPGRADE PATH `n BASIC `n $10,000 `n $1,000 `n Email + phone verified `n Immediate on signup `n ENHANCED (Tier 2) `n $250,000 `n $5,000 `n Gov ID + liveness + address `n Onfido — automated in-app `n INSTITUTIONAL `n Unlimited `n Unlimited `n Entity docs + accredited investor `n Manual review — contact Genesis ops `n `n 2.5 SLAS, UPTIME & SUPPORT `n SLA METRIC `n TARGET `n MEASUREMENT `n Platform uptime `n 99.5% `n Monthly availability of API + BFF `n P50 transaction latency `n <200ms `n BFF aggregate endpoint median `n P99 transaction latency `n <1,000ms `n BFF aggregate endpoint 99th percentile `n FX quote delivery `n <300ms `n remittance.service .ts quote latency `n Order settlement (digital corridors) `n <2 hours `n Time from order → SETTLED status `n KYC decision (automated) `n <5 minutes `n Onfido webhook → compliance activation `n Incident response (P1) `n <2 hours resolution `n From PagerDuty alert to resolution `n Harvest cycle `n Every 15 minutes `n harvest.cron .ts with PagerDuty alerting on failure `n Reconciliation `n Hourly check `n reconciliation.service .ts — alert if delta >$0.01 `n `n `n `n `n SECTION III — INVESTORS `n Market opportunity · Business model · Technology moat · Traction · Path to scale `n `n 3.1 THE OPPORTUNITY `n The global remittance market processed $905 billion in 2024 and is projected to reach $1.33 trillion by 2032 . Every dollar of that volume spends 1–3 days sitting idle between sender and recipient — earning nothing. Genesis Reserve is the first platform purpose-built to monetize that idle capital through institutional DeFi yield infrastructure, while simultaneously operating as a compliant, partner-facing remittance and treasury platform. `n `n THE MARKET GAP `n SIZE `n GENESIS RESERVE'S POSITION `n Industry float earning near-zero `n ~$3.7B daily at any moment `n Our yield infrastructure — AWS model for remittance float `n Annual yield left uncaptured `n ~$196M/yr across industry `n Partner revenue share + Genesis spread `n Digital remittance market `n $30.4B in 2025 → $96.8B by 2033 `n B2B infrastructure layer — not competing for retail users `n Stablecoin yield market `n Grew 13x Aug 2023 → May 2025 `n First compliant institutional USDC yield platform `n Global remittance CAGR `n ~8% through 2030 `n Recurring infrastructure revenue scales with market `n `n 3.2 BUSINESS MODEL — HOW WE MAKE MONEY `n REVENUE STREAM `n MECHANISM `n YEAR 1 ESTIMATE `n SCALABILITY `n Yield spread (AUM) `n 1.5%/yr on deployed USDC — retain 0.5% after partner share `n $1.5M at $300M AUM `n Linear with AUM `n Transaction fees `n 0.42% per transfer — retain 60–100% depending on partner tier `n $630k–$1.26M at $150M/yr volume `n Linear with volume `n FX spread `n 0.25% on every cross-border send — 100% Genesis `n $250k at $100M/yr cross-border `n Grows with corridor expansion `n Future: Card program `n Interchange + $3/card (Stripe Issuing) `n $200k+ at 5k active cards `n Per-user SaaS-like revenue `n Future: Partner API access `n Tiered API pricing for programmatic access `n TBD post-MVP `n High-margin recurring revenue `n `n YEAR 1 REVENUE SCENARIO: $500M AUM × 0.5% spread = $2.5M | $150M annual send volume × 0.42% × avg 70% retention = $441k–$882k | $100M cross-border × 0.25% = $250k | Total Year 1 conservative estimate: $3.2–4.1M revenue at target scale. Zero marginal cost of new deposits — yield infrastructure is already running. `n `n 3.3 TECHNOLOGY MOAT `n MOAT DIMENSION `n DESCRIPTION `n COMPETITIVE BARRIER `n Live DeFi Infrastructure `n 5 deployed smart contracts on Arbitrum One — ERC-4626 vault, StrategyRouter , 4 adapters, ComplianceRegistry `n Takes 6–12 months to replicate — each adapter requires separate audit and integration `n 15-Minute Harvest Cycle `n Automated yield collection every 900 seconds — no competitor operates at this cadence for institutional clients `n Operational infrastructure — cannot be bought; must be built and proven over time `n Compliance Architecture `n On-chain KYC state ( ComplianceRegistry.sol ) + 4-gate screening + TRISA + hash-chained ledger `n Regulatory approval takes years — our stack is already compliant and auditable `n Aggregate BFF Model `n One API call composes balance + yield + compliance + orders — 40–60% latency advantage `n Platform-specific optimization that compounds with every new feature `n GENIUS Act Positioning `n The July 2025 GENIUS Act prohibits stablecoin issuers from paying yield — explicitly permits third-party platforms `n Regulatory moat: Circle and PayPal cannot legally do what Genesis Reserve does for their users `n 12,517 Lines of Production Code `n 2,741 Solidity + 4,103 backend + 4,190 frontend + 1,483 tests — complete, production-grade platform `n 18–24 months of engineering work already complete `n `n 3.4 PRODUCT TRACTION — WHAT IS LIVE TODAY `n MILESTONE `n STATUS `n DATE `n GenesisVault.sol deployed — Arbitrum One mainnet `n `n March 2026 `n StrategyRouter.sol deployed + 4 adapters registered `n `n March 2026 `n ComplianceRegistry.sol deployed — on-chain KYC `n `n March 2026 `n Onfido + Chainalysis webhooks — compliance automation `n `n March 2026 `n 19-route Express API — treasury/remittance/ledger/compliance `n `n March 2026 `n 15-min harvest cron — PagerDuty + Datadog wired `n `n March 2026 `n Complete frontend — Privy wallet + Yield Engine UI `n `n March 2026 `n E2E test suite — deposit → reserve → payout `n `n March 2026 `n 10 business partners signed for MVP launch `n `n Q2 2026 `n First canary launch — 2 partners, 100 users `n `n April 2026 `n Smart contract audit ( Certik /Trail of Bits) `n `n Q2 2026 `n $10M AUM canary milestone `n `n May 2026 `n $500M AUM at scale `n `n Post-audit `n `n 3.5 PATH TO SCALE — THREE HORIZONS `n HORIZON `n TIMELINE `n TRIGGER `n AUM TARGET `n REVENUE ESTIMATE `n `n Q2 2026 `n Audit complete + 2 partners live `n $10M `n $50–100k ARR `n `n Q3–Q4 2026 `n 10 partners + MoneyGram live `n $100M `n $500k–1M ARR `n `n 2027 `n Partner API + white-label + 20+ corridors `n $500M+ `n $3–5M ARR `n `n 2028 `n Become the yield layer for remittance industry `n $5B+ `n $25M+ ARR `n `n THE INFRASTRUCTURE PLAY: Genesis Reserve is not a remittance company — it is the yield infrastructure that every remittance company needs. The 10 companies in our partner pipeline ( Remitly , Wise, MoneyGram, Western Union, Ria, Intermex , WorldRemit , Viamericas , Xoom, Nium ) represent $196M in annual float yield we can capture as infrastructure revenue. One enterprise contract at MoneyGram's scale ($200B/yr volume) alone represents $43M in annual yield through our platform. `n `n 3.6 USE OF FUNDS `n ALLOCATION `n AMOUNT `n PURPOSE `n Smart Contract Audit `n $40–80k `n Certik or Trail of Bits — required before >$10M AUM `n MoneyGram API Integration `n $25–50k eng cost `n Live payout wiring — unlocks the send flow for real users `n BFF + Missing Components (6 items) `n $30–60k eng cost (internal) `n Week 1–2 sprint to close architecture gaps `n Compliance Licensing (FinCEN MSB) `n $10–15k + 4 weeks `n Required for US money transmission `n Marketing + Partner Onboarding `n $50–100k `n Onboard 10 partners, acquire first 2,000 users `n Infrastructure Scaling (6 months) `n $150–300k `n Cloud + DB + monitoring for $100M AUM `n Operations Buffer (6 months) `n $50–100k `n On-call + support + compliance ops `n `n `n `n `n `n `n Genesis Reserve · Official Technical Specifications v2.0 · March 26, 2026 · CONFIDENTIAL — NOT FOR DISTRIBUTION `n 
